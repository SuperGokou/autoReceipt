services:
  - type: web
    name: autoReceipt
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DASHSCOPE_API_KEY
        sync: false
      - key: FORMSPREE_ENDPOINT
        sync: false
